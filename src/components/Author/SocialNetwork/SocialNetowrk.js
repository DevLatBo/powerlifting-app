const SocialNetwork = (props) => {
    return (
        <div>
            <a href="#">Facebook</a>
            <a href="#">Twitter</a>
            <a href="#">Youtube</a>
        </div>
    )
};

export default SocialNetwork;